#ifndef CRAZYPOD_METADATA_STUB_CONFIG_H
#define CRAZYPOD_METADATA_STUB_CONFIG_H
/* rbcodec's own host config, without the firmware system.h the warble
 * harness pulls in: the metadata parsers need neither. */
#include "../../lib/rbcodec/rbcodecconfig-example.h"
#endif
